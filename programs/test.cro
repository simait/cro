module test {
	class root() {
		switch = new switch();
		method init():void {
			send switch.disable();
			switch.disable();
			switch.enable();
			switch.disable();
			switch.enable();
		}
	}
	class switch() {
		var enabled:bool := false;

		method enable():void {
			enabled := true;
		}

		method disable():void {
			if (not enabled) {
				send notification();
			}
			enabled := false;
		}

		method notification():void {
			print("Double disable!\r\n");
		}
	}
}