module test {
	class root(dummy:int) {
		test = new test(0);
		method init(dummy:int):void {
			test.test(0);
			sum:int := 0;
		}
	}

	class test(dummy:int) {
		method test(dummy:int):int {
			array:int[10];
			sum:int := 0;
			foreach i in array {
				sum := sum + i;
			}
		}
	}
}